class Solution(object):
    def removeElement(self, nums, val):
        """
        :type nums: List[int]
        :type val: int
        :rtype: int
        """
        left = 0
        for right in nums:
            if right != val:
                nums[i] = x
                i += 1
        return i
